{
  "hooks": {
    "SessionStart": [
      {
        "command": "git submodule update --init --recursive",
        "description": "Initialize git submodules (gstack skills)"
      },
      {
        "command": "command -v graphify >/dev/null 2>&1 && (nohup graphify update . >/dev/null 2>&1 &) || true",
        "description": "Refresh graphify code graph in background (AST-only, no LLM, non-blocking)"
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Glob|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f graphify-out/graph.json ] && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"graphify: Knowledge graph exists. Read graphify-out/GRAPH_REPORT.md for god nodes and community structure before searching raw files.\"}}' || true"
          }
        ]
      }
    ]
  }
}
