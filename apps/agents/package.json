{
  "name": "@trustloop/agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "lint": "biome check src/",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "@mastra/core": "latest",
    "@ai-sdk/openai": "^2.0.89",
    "@shared/database": "*",
    "@shared/env": "*",
    "@shared/rest": "*",
    "@shared/types": "*",
    "hono": "^4.0.0",
    "@hono/node-server": "^1.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
