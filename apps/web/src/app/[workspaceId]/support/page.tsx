"use client";

import { SupportInbox } from "@/components/support/support-inbox";

/**
 * Workspace-scoped support inbox entrypoint.
 */
export default function WorkspaceSupportPage() {
  return <SupportInbox />;
}
